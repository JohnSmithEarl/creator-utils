export class cu_Graphics {
    constructor() {

    }
};

// let UtilGraphics = {};

// UtilGraphics.clear = function (graphics) {
//     graphics.clear();
// };

// UtilGraphics.drawLine = function (graphics, fromP = cc.v2(), toP = cc.v2(), lineWidth = 2, color = cc.Color.BLACK) {
//     graphics.lineWidth = lineWidth;
//     graphics.strokeColor = color;
//     graphics.moveTo(fromP.x, fromP.y);
//     graphics.lineTo(toP.x, toP.y);
//     graphics.stroke();
// };

// UtilGraphics.drawCircle = function (graphics, P = cc.v2(), radius = 6, color = cc.Color.BLACK) {
//     graphics.fillColor = color;
//     graphics.circle(P.x, P.y, radius);
//     graphics.fill();
// };

// UtilGraphics.drawBezier = function (graphics, P0 = cc.v2(), P1 = cc.v2(), P2 = cc.v2(), P3 = cc.v2(), lineWidth = 2, color = cc.Color.BLACK) {
//     graphics.lineWidth = lineWidth;
//     graphics.strokeColor = color;
//     graphics.moveTo(P0.x, P0.y);
//     graphics.bezierCurveTo(P1.x, P1.y, P2.x, P2.y, P3.x, P3.y);
//     graphics.stroke();
// };

// module.exports = UtilGraphics;